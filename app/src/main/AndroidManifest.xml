<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hphp.knowgroup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WellcomeActivity" />
        <activity android:name=".ListaRamos" />
        <activity
            android:name=".GrupoListActivity"
            android:label="@string/title_grupo_list"></activity>
        <activity
            android:name=".GrupoDetailActivity"
            android:label="@string/title_grupo_detail"
            android:parentActivityName=".GrupoListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.hphp.knowgroup.GrupoListActivity" />
        </activity>
    </application>

</manifest>